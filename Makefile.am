ACLOCAL_AMFLAGS = -I m4 ${ACLOCAL_FLAGS}
AM_MAKEFLAGS = --no-print-directory

SUBDIRS = lib firrtlator

#pkgconfigdir = $(datadir)/pkgconfig
#pkgconfig_DATA = firrtlator.pc
#EXTRA_DIST = firrtlator.pc.in
#CLEANFILES = firrtlator.pc